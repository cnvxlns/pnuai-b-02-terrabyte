# === Node / Frontend (Expo, Next.js) ===
node_modules/
.expo/
dist/
web-build/
.next/
out/
*.tsbuildinfo
expo-env.d.ts
.metro-health-check*

# === Java / Spring Boot (backend) ===
backend/build/
backend/target/
backend/.gradle/
backend/out/
backend/gradle.properties
*.class
*.jar
!gradle/wrapper/gradle-wrapper.jar
*.war

# === Embedded / Firmware (edge) ===
edge/.pio/
edge/build/
*.hex
*.bin
*.elf

# === Environment / Secrets ===
.env
.env.*
!.env.example
*.pem
*.key
*.jks
*.p8
*.p12
*.mobileprovision
application-local.yml
application-local.properties

# === Logs ===
logs/
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# === IDE / Editor ===
.idea/
*.iml
.vscode/
!.vscode/extensions.json
!.vscode/settings.json

# === OS ===
.DS_Store
Thumbs.db
desktop.ini

# === AI agent working dirs ===
.agent-runs/
.claude/
HANDOFF.md
docs/superpowers/

# 합성 벤치 캡처는 tools/make_bench_capture.py로 재생성한다.
edge/pi/data/raw/

# TLS 인증서는 infra/mosquitto/generate-certs.sh 로 각자 만든다.
# ca.key / server.key 가 여기 생기므로 저장소에 절대 들어가면 안 된다.
infra/mosquitto/certs/
