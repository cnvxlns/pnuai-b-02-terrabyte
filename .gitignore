# === Node / Frontend (Expo, Next.js) ===
node_modules/
.expo/
dist/
web-build/
.next/
out/
*.tsbuildinfo
expo-env.d.ts
.metro-health-check*

# === Java / Spring Boot (backend) ===
backend/build/
backend/target/
backend/.gradle/
backend/out/
backend/gradle.properties
*.class
*.jar
!gradle/wrapper/gradle-wrapper.jar
*.war

# === Embedded / Firmware (edge) ===
edge/.pio/
edge/build/
*.hex
*.bin
*.elf

# === Environment / Secrets ===
.env
.env.*
!.env.example
*.pem
*.key
*.jks
*.p8
*.p12
*.mobileprovision
application-local.yml
application-local.properties

# === Logs ===
logs/
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# === IDE / Editor ===
.idea/
*.iml
.vscode/
!.vscode/extensions.json
!.vscode/settings.json

# === OS ===
.DS_Store
Thumbs.db
desktop.ini

# === AI agent working dirs ===
.agent-runs/
.claude/
HANDOFF.md
docs/superpowers/

# 합성 벤치 캡처는 tools/make_bench_capture.py로 재생성한다.
edge/pi/data/raw/

# 관수량 AI 서버는 폐기됐다 — 산출을 엣지 물수지 수식으로 옮겼다.
# 근거는 docs/design/irrigation_volume.md, 코드는 feature/ai-server 브랜치에 남아 있다.
# 로컬 작업 디렉터리(.venv·모델 아티팩트)가 실수로 커밋되지 않게 막는다.
ai-server/
